[project]
name = "Multiagent-poc-project"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "bs4>=0.0.2",
    "fastapi[standard]",
    "feedparser>=6.0.11",
    "mcp[cli]>=1.9.1",
    "openai-agents>=0.0.17",
]
